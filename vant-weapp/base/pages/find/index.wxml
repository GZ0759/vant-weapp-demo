<view class="main">
  <view class="main-btns" bind:tap="onCharacter">
    <view class="main-btn" wx:for="{{ 12 }}" class="main-btn" mark:num="{{ index }}">
      <van-button custom-class="btn" plain="{{ num !== index }}" type="info">A</van-button>
    </view>
  </view>
</view>